export const createAutor = async (autorData: any) => {
    // lógica para fazer a requisição ao backend para criar o autor
};

export const createLivro = async (livroData: any) => {
    // lógica para fazer a requisição ao backend para criar o livro
};

export const createEmprestimo = async (emprestimoData: any) => {
    // lógica para fazer a requisição ao backend para criar o empréstimo
};
